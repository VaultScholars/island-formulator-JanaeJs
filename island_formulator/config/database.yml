default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: island_formulator_development

test:
  <<: *default
  database: island_formulator_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
